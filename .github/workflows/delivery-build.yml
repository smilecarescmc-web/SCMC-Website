name: Delivery Branch Build

on:
  push:
    branches:
      - delivery-structural-fix
  workflow_dispatch:

concurrency:
  group: delivery-structural-fix-build
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run build
